import { FooterPrototype } from "./FooterPrototype";

export const metadata = {
  title: "Footer 布局原型",
  robots: { index: false, follow: false },
};

export default function FooterPrototypePage() {
  return <FooterPrototype />;
}
